---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Examples - Code Bits"
  description="Code Bitsのパーツを組み合わせた実用的なサンプルページ集。"
>
  <section class="examples-page">
    <div class="container">
      <header class="page-header">
        <h1 class="page-title">Examples</h1>
        <p class="page-description">
          Code Bitsのパーツを組み合わせた、実用的なサンプルページ集です。<br>
          実際のWebサイトでどのように活用できるかを確認できます。
        </p>
      </header>

      <div class="examples-grid">
        <!-- Beauty Salon LP -->
        <article class="example-card">
          <a href="/examples/beauty-salon" class="example-card-link">
            <div class="example-card-thumbnail">
              <iframe src="/examples/beauty-salon/index.html" loading="lazy" title="Beauty Salon LP"></iframe>
            </div>
            <div class="example-card-body">
              <span class="example-card-label">Landing Page</span>
              <h2 class="example-card-title">Beauty Salon LP</h2>
              <p class="example-card-description">
                美容サロン向けランディングページ。Glowing ButtonとFlip Cardを活用。
              </p>
              <div class="example-card-tags">
                <span class="tag">#005 Glowing Button</span>
                <span class="tag">#003 Flip Card</span>
              </div>
            </div>
          </a>
        </article>

        <!-- More examples can be added here -->
        <div class="example-placeholder">
          <div class="placeholder-icon">📝</div>
          <p>新しいサンプルページを追加予定</p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .examples-page {
    padding: 4rem 0;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .page-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .page-title {
    font-size: var(--text-4xl);
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--color-text);
  }

  .page-description {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  .examples-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
  }

  .example-card {
    background: var(--color-bg);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-md);
    transition: all 0.3s;
  }

  .example-card:hover {
    box-shadow: var(--shadow-xl);
    transform: translateY(-4px);
  }

  .example-card-link {
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .example-card-thumbnail {
    width: 100%;
    height: 250px;
    overflow: hidden;
    background: var(--color-bg-secondary);
    position: relative;
  }

  .example-card-thumbnail iframe {
    width: 100%;
    height: 100%;
    border: none;
    pointer-events: none;
    transform: scale(0.5);
    transform-origin: top left;
    width: 200%;
    height: 200%;
  }

  .example-card-body {
    padding: 1.5rem;
  }

  .example-card-label {
    display: inline-block;
    background: var(--color-primary);
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-sm);
    font-size: var(--text-xs);
    margin-bottom: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .example-card-title {
    font-size: var(--text-xl);
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--color-text);
  }

  .example-card-description {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: var(--leading-normal);
    margin-bottom: 1rem;
  }

  .example-card-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
  }

  .example-placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 400px;
    background: var(--color-bg-secondary);
    border-radius: var(--radius-lg);
    border: 2px dashed var(--color-border);
    color: var(--color-text-secondary);
  }

  .placeholder-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  @media (max-width: 768px) {
    .examples-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
